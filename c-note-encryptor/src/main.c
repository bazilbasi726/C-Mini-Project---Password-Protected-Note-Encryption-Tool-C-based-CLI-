#include "encrypt.h"
#include "loglist.h"
#include "types.h"

#include <stdio.h>

int main(void) {
    Flags flags = {0};
    NoteInfo info = {0};
    LogNode *log_head = NULL;

    (void)flags;
    (void)info;
    (void)log_head;

    puts("c-note-encryptor scaffold ready (logic not yet implemented).");
    return 0;
}
